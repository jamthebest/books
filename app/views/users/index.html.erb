<h1>Usuarios</h1>

<table>
  <tr>
    <th>Email</th>
    <th>Username</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.email %></td>
    <td><%= user.username %></td>
    <td><a class="btn btn-info" href=<%=user_path(user)%>><i class="icon-eye-open icon-black"></i> Mostrar </a></td>
  </tr>
<% end %>
</table>

<br />
<a class="btn btn-primary" href=<%=new_user_path%>> Nuevo Usario </a><br>
<br><a class="btn btn-inverse" href=<%=root_url%>><i class="icon-arrow-left icon-white"></i> Atrás </a>